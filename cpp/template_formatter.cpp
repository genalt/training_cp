#include <iostream>
#include <vector>
#include <cmath>
#include <set>
#include <map>
#include <algorithm>
#include <queue>
#include <deque>
#include <cstdio>

using namespace std;
typedef long long ll;
typedef vector<int> vi;
typedef vector<long long> vl;


int main() {
    ios::sync_with_stdio(false);

    freopen("segment_tree.cpp", "r", stdin);
    string line;
    while(getline(cin, line)) {
        cout << "\"" << line << "\"," << endl;
    }
    return 0;
}